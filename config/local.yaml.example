# Copy to config/local.yaml (gitignored) to override defaults
topic:
  mode: trending
  filter:
    includeKeywords:
      - AI
      - science
schedule:
  timezone: America/New_York
  preferredHourLocal: 9
